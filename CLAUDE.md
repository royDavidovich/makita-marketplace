# makita-marketplace Development Guidelines

Auto-generated from all feature plans. Last updated: 2026-03-01

## Active Technologies

- TypeScript — Node.js 20 (backend) + React 18 (frontend) + Express, Prisma (SQLite), Playwright, TanStack Query, React Router v6, Tailwind CSS, node-cron (001-price-comparison)

## Project Structure

```text
backend/
frontend/
tests/
```

## Commands

npm test; npm run lint

## Code Style

TypeScript — Node.js 20 (backend) + React 18 (frontend): Follow standard conventions

## Recent Changes

- 001-price-comparison: Added TypeScript — Node.js 20 (backend) + React 18 (frontend) + Express, Prisma (SQLite), Playwright, TanStack Query, React Router v6, Tailwind CSS, node-cron

<!-- MANUAL ADDITIONS START -->

## Workflow Preferences

- **Commit messages**: Always use a meaningful, descriptive commit message — even when the user simply says "commit changes". Follow the Conventional Commits format: `type(scope): short description`. Never use generic messages like "commit changes" or "update files".

<!-- MANUAL ADDITIONS END -->
