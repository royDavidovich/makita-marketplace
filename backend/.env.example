# Database (SQLite file path, relative to backend/)
DATABASE_URL="file:./data/makita.db"

# Secret for protecting the manual scrape trigger endpoint
SCRAPE_SECRET="change-me-in-production"

# Port the backend listens on
PORT=3001
